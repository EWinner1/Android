<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FF9900" />

    <corners android:radius="10dp" />

    <gradient
        android:centerColor="#FFFF00"
        android:endColor="#00FF00"
        android:startColor="#FF5722" />

</shape>

    <!--
    1、solid
        指定内部填充色
            android:color="@android:color/white"
    2、Corners
        指定圆角半径
            android:radius 四个角半径值
            android:topLeftRadius左上角半径值
            android:topRightRadius右上角半径值
            android:bottomLeftRadius右下角半径值
            android:bottomRightRadius左下角半径值
            <corners
            android:bottomLeftRadius="10dp"
            android:bottomRightRadius="10dp"
            android:radius="10dp"
            android:topLeftRadius="10dp"
            android:topRightRadius="10dp" />
    3、Gradient
        指定gradient用以定义渐变色，可以定义两色渐变和三色渐变，及渐变样式，它的属性有下面几个：
            android:startColor渐变起始颜色
            android:endColor渐变结束颜色
            android:centerColor渐变中间颜色
            android:angle 渐变的角度，angle=0时，渐变色是从左向右，然后逆时针方向转；当angle=90时，渐变色从上往下。angle必然是45的倍数
            android:type 渐变类型：linear(线性)、radial(放射性，以开始色为中心)、sweep(扫描线式渐变) android:userLevel如果要使用LevelListDrawable对象，就要设置为true。设置true无渐变色，false有渐变色
            android:grdientRadial渐变半径，只有设置type为radial时，此值才生
            默认值是0.5，有效值是0.0~1.0，超出该范围后会看不出渐变效果。centerX、centerY的取值其实是宽和高的百分比。
            android:centerX 渐变中心X点坐标的相对位置
            android:centerY 渐变中心Y点坐标的相对位置
    4、Stroke 
            android:width="20dp":描边的宽度
            android:color="#fff"：描边的颜色
            android:dashGap="1dp"：虚线的间隔
            android:dashWidth="10dp"：虚线的宽度，为0时为实线
    5、size
        用来定义图片的大小，基本不怎么使用
            size
            android:width="10dp"
            android:height="10dp"
    6、padding
        用来定义内部边距。
            padding
            android:left="dimension"
            android:top="dimension"
            android:right="dimension"
            android:bottom="dimension"
    -->